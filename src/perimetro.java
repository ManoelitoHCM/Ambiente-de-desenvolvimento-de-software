package src;

import utils.Calculadora;

public class perimetro {

    Calculadora calculadora = new Calculadora();

    public double PerimetroRetangulo(double lado1, double lado2) {
		  return calculadora.soma(lado1, lado2);
	}
}